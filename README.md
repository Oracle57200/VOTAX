# VOTAX
votax.js is a lightweight JavaScript utility library that provides powerful CRUD operations, validation, persistence, UI helpers, and GSAP-style animation utilities for modern web applications — without any external dependencies.
